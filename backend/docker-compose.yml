version: '3.8'

services:
  memgraph:
    image: memgraph/memgraph:latest
    container_name: memgraph_linguistic
    ports:
      - "7687:7687"
      - "7444:7444"
    volumes:
      - memgraph-data:/var/lib/memgraph
    restart: unless-stopped

volumes:
  memgraph-data:
    driver: local